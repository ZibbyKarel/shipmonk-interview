{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Write|Edit",
                "hooks": [
                    {
                        "type": "command",
                        "command": "graphify update . 2>/dev/null",
                        "async": true,
                        "statusMessage": "Updating graphify knowledge graph..."
                    },
                    {
                        "type": "command",
                        "command": "cat | python3 -c \"import sys,json; d=json.load(sys.stdin); fp=d.get('tool_input',{}).get('file_path',''); exit(0 if fp.endswith('.java') else 1)\" && ./mvnw spotless:apply -q 2>/dev/null || true",
                        "async": true,
                        "statusMessage": "Formatting Java files..."
                    }
                ]
            }
        ],
        "PreToolUse": [
            {
                "matcher": "Glob|Grep",
                "hooks": [
                    {
                        "type": "command",
                        "command": "[ -f graphify-out/graph.json ] && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"graphify: Knowledge graph exists. Read graphify-out/GRAPH_REPORT.md for god nodes and community structure before searching raw files.\"}}' || true"
                    }
                ]
            }
        ]
    }
}
